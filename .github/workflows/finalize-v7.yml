name: Finalize V7 Release
on:
  pull_request:
    branches: [main]
permissions:
  contents: write
jobs:
  finalize-v7:
    if: >-
      github.event.pull_request.head.ref == 'agent/finalize-v7-release' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate, tag and publish V7
        env:
          GH_TOKEN: ${{ github.token }}
        run: bash .github/scripts/finalize-v7.sh
